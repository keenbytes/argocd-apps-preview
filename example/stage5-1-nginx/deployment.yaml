apiVersion: apps/v1
kind: Deployment
metadata:
  name: stage5-1-nginx
  labels:
    app: stage5-1-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stage5-1-nginx
  template:
    metadata:
      labels:
        app: stage5-1-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
