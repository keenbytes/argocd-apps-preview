apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configName }}
  labels:
    app: {{ .Release.Name }}
data:
{{- range $key, $value := .Values.configData }}
  {{ $key }}: "{{ $value }}"
{{- end }}