# Secrets

Additional flag `--secrets` can be passed with a directory which contains secrets that must be applied after the 
manifests. These could be access to git repositories, image registries, etc.

